# Domain-generation experiment

Input: `../../ujg/workshop-registration.ujg.jsonld`

Goal: evaluate whether a model can derive the minimal domain requirements implied by the UJG without under- or over-specifying the workshop system.

Do not generate backend code in this stage. Generated artifacts belong under `runs/`. A reviewed technology-neutral result will later be frozen under `reference/` and realized as the reference backend in `../../domain/reference/`.

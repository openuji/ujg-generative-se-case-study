import manifestData from "virtual-design-tokens-manifest";
import type { DesignTokensManifest } from "../tokens/resolve-tokens";

const manifest: DesignTokensManifest = manifestData;

export default manifest;
